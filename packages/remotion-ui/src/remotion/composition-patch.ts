export async function patchRootTsx(
  rootPath: string,
  compositionImport: string,
): Promise<void> {
  // TODO: Register new <Composition> entry in Root.tsx
  void rootPath;
  void compositionImport;
}
