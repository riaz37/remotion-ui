---
title: Marker Highlight
description: Highlighter-pen-style wipe animation behind the target word.
---

import { MarkerHighlightPreview } from '@/components/previews/marker-highlight';

<ComponentPage name="marker-highlight" preview={MarkerHighlightPreview} durationInFrames={60}>

Finds `highlightWord` in `text` and animates a highlighter-pen-style wipe behind it using `spring()`. Use `markerColor` and `invertOnHighlight` for highlighter-style emphasis.

</ComponentPage>
