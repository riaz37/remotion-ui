---
title: Parallax Layers
description: Depth-offset layers on one driver. Install with npx remotion-ui@latest add parallax-layers.
---

import { ParallaxLayersPreview } from '@/components/previews/parallax-layers';

<ComponentPage name="parallax-layers" preview={ParallaxLayersPreview} durationInFrames={120}>

Depth-offset layers on one driver. Multi-layer, unlike zoom-pan-frame's single still.

## Usage

```tsx
import { ParallaxLayers } from "@/remotion/primitives/parallax-layers";

<ParallaxLayers />
```

</ComponentPage>
