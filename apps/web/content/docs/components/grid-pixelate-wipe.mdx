---
title: Grid Pixelate Wipe
description: Grid cell opacity stagger wipe for TransitionSeries.
---

import { TransitionGridPixelateWipePreview } from "@/components/previews/transition-grid-pixelate-wipe";

<ComponentPage name="grid-pixelate-wipe" preview={TransitionGridPixelateWipePreview} durationInFrames={90}>

Exports `transitionGridPixelateWipe()` and `getTransitionGridPixelateWipeDuration()` for use with `TransitionSeries.Transition`.

Requires `@remotion/transitions`. Run `npx remotion add @remotion/transitions` if not already installed.

</ComponentPage>
