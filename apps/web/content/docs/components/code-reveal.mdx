---
title: Code Reveal
description: Editor window that writes a syntax-highlighted file, then focuses the lines that matter.
---

import { CodeRevealPreview } from '@/components/previews/code-reveal';

<ComponentPage name="code-reveal" preview={CodeRevealPreview} durationInFrames={165}>

An editor writes the file in front of the viewer — characters land under a live caret, syntax colours as it goes — and once the listing is complete, the highlighted lines take focus while the rest recedes.

</ComponentPage>
