---
title: Animated Bar Chart
description: Ranked bar chart scene with a real value axis.
---

import { AnimatedBarChartPreview } from '@/components/previews/animated-bar-chart';

<ComponentPage name="animated-bar-chart" preview={AnimatedBarChartPreview}>

Bars are measured against a rounded axis rather than the largest value, so the
longest bar stops short of the frame edge and the ticks under it read as a
scale. Each bar's length and its counter share one spring, and `highlightLabel`
lifts the row you are actually talking about.

</ComponentPage>
