---
title: Quote Card
description: A pull quote read aloud — mark, lines, marker sweep, attribution.
---

import { QuoteCardPreview } from '@/components/previews/quote-card';

<ComponentPage name="quote-card" preview={QuoteCardPreview} durationInFrames={140}>

The quote mark draws, the quote rises line by line out of its own masks, a
marker sweeps the `emphasis` phrase word by word, and the attribution arrives
last with its initials disc.

`emphasis` is matched over the whole quote and sliced per line, so a phrase that
spans a line break still reads as one stroke.

</ComponentPage>
