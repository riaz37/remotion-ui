---
title: Donut Chart
description: Multi-segment donut with labels. Install with npx remotion-ui@latest add donut-chart.
---

import { DonutChartPreview } from '@/components/previews/donut-chart';

<ComponentPage name="donut-chart" preview={DonutChartPreview} durationInFrames={120}>

Multi-segment donut with labels. Narrowed from "progress ring" — stat-card already owns the single-value ring.

## Usage

```tsx
import { DonutChart } from "@/remotion/primitives/donut-chart";

<DonutChart />
```

</ComponentPage>
