---
title: Zoom Through
description: Camera push-through zoom transition for TransitionSeries.
---

import { TransitionZoomThroughPreview } from "@/components/previews/transition-zoom-through";

<ComponentPage name="zoom-through" preview={TransitionZoomThroughPreview} durationInFrames={90}>

Exports `transitionZoomThrough()` and `getTransitionZoomThroughDuration()` for use with `TransitionSeries.Transition`.

Requires `@remotion/transitions`. Run `npx remotion add @remotion/transitions` if not already installed.

</ComponentPage>
