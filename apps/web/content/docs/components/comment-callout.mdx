---
title: Comment Callout
description: A viewer comment answered on screen — marked up, hearted, and replied to.
---

import { CommentCalloutPreview } from '@/components/previews/comment-callout';

<ComponentPage name="comment-callout" preview={CommentCalloutPreview} durationInFrames={165}>

The comment lands from the feed, the marker sweeps the phrase named by `highlight`, the creator hearts it, and the composer opens and types out `reply` before it sends. Leave `reply` empty to end the scene on the comment itself, and drop `highlight` to skip the markup beat.

</ComponentPage>
