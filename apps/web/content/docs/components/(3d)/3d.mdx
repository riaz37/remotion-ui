---
title: "3D"
description: "Lit WebGL scenes built on @remotion/three, rendered deterministically frame by frame."
---

3D components are real React Three Fiber scenes: modelled geometry, image-based lighting and contact shadows, with every move driven by the frame number so a render is identical on every run. Render them with `--gl=angle`.

<CategoryGrid category="3d" />
