---
title: Spatial Push
description: Perspective push transition for TransitionSeries.
---

import { TransitionSpatialPushPreview } from "@/components/previews/transition-spatial-push";

<ComponentPage name="spatial-push" preview={TransitionSpatialPushPreview} durationInFrames={90}>

Exports `transitionSpatialPush()` and `getTransitionSpatialPushDuration()` for use with `TransitionSeries.Transition`.

Requires `@remotion/transitions`. Run `npx remotion add @remotion/transitions` if not already installed.

</ComponentPage>
